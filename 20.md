<h1>不要令可复制或移动类型的数据成员为 const 或引用</h1>

`const` 和引用数据成员在可复制或移动类型中没什么用处，还会由于微妙的原因使这种类型变得至少部分地无法复制/无法移动而很难使用。

<h2>理由</h2>

    class bad {
        const int i;    // 不好
        string& s;      // 不好
        // ...
    };

`const` 和 `&` 数据成员会导致类变成“仅可进行某些复制”——可复制构造但不可复制赋值。